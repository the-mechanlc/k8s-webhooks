apiVersion: v1
kind: Pod
metadata:
  name: pod-without-team-label
  labels:
    app: myapp        # ❌ missing the 'team' label — should be REJECTED
spec:
  containers:
    - name: nginx
      image: nginx:alpine
  restartPolicy: Never
