apiVersion: v1
kind: Pod
metadata:
  name: pod-with-team-label
  labels:
    team: backend     # ✅ has the required 'team' label — should be ADMITTED
spec:
  containers:
    - name: nginx
      image: nginx:alpine
  restartPolicy: Never
